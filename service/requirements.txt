fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
# optional — only needed for the LLM estimator path:
anthropic>=0.40
# optional — only needed when implementing GoogleCalendarProvider:
# google-api-python-client
# google-auth
# google-auth-oauthlib
